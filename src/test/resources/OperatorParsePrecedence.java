
class OperatorTest {
    public static void main(String[] args) {
        int sub = (1 - -2);
        boolean bool = -1 == true;
        int aa = 1 * false;
        int a = 1 * 2 + 3;
        int b = 1 + 2 * 3;
        int c = (1 + 2) * false;
        int d = -(1 + 2) * 3;
        int e = 1 + -2 * 3;
        boolean g = 1 + 3 < 4;
        boolean f = -1 > 2 * -4;
        boolean h = true && false || false;
        boolean f = true || 2 < 3 && !false;
        boolean z = false && 5 > 1 * -3 || true;
        boolean z = false && (5 > 1 * -3 || true);
        FacMethod mthd = new FacMethod();
        FacMethod mthd = new FacMethod();
        boolean z = 5 == false;
        while (a) {
            int x = 1 + 2;
        }
        FacMethod mthd = new FacMethod();
        while (true) {

        }
        while (
                true
                )

        {

        }
        while (true) {
        }
        int retval = num * this.factorial(num - 1);
        int lhs = 0;
        int rhs = 2;
        retval = lhs - lhs / rhs * rhs;
    }
}